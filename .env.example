# Copy to .env.local — do NOT commit secrets.
# 完整跨專案 secret 對照表與命名規範見 CLAUDE.md 第 7 節。

# === 前端必需（Vite；缺這兩個 app 會連不上 DB）===
# Furniture 1000 project = riaubhtruisbwdlwjzur
VITE_SUPABASE_URL=https://riaubhtruisbwdlwjzur.supabase.co
VITE_SUPABASE_ANON_KEY=your_furniture_anon_key
SUPABASE_PROJECT_REF=riaubhtruisbwdlwjzur

# === 前端選用 ===
# PMS v3 project (kqwktnplkqucsbasyfjl) 的 anon key —— 注意「MASTER」= PMS v3，不是 Furniture
VITE_MASTER_SUPABASE_ANON_KEY=
# PMS SSO 登入按鈕；未設定時 LoginView 的「使用 PMS 登入」會 disabled
# 例：https://bwteam-project.com/api/bwf/sso/start（repo: chifung-BWSolution/PMS3.0）
VITE_PMS_SSO_START_URL=

# === scripts/*.mjs 用（本機一次性維護腳本，不要放進前端）===
# Supabase Management API PAT（sbp_…）；只放環境變數，不要寫入檔案
SUPABASE_ACCESS_TOKEN=
# Furniture 1000 的 service_role key
SUPABASE_SERVICE_ROLE_KEY=
# PMS v3 的 service_role key（跨專案讀 bwf_pitchings / customers 用）
MASTER_SERVICE_ROLE_KEY=
MASTER_SUPABASE_URL=https://kqwktnplkqucsbasyfjl.supabase.co

# ⚠️ scripts/backfill-quote-client-contact.mjs 的 Furniture URL fallback 鏈是
# BWF_SUPABASE_URL → VITE_SUPABASE_URL → NEXT_PUBLIC_SUPABASE_URL。
# NEXT_PUBLIC_SUPABASE_URL 在其他 repo 指向別的 project，若前兩個沒設會寫錯資料庫。
# 跑這支腳本前務必確認 VITE_SUPABASE_URL 已設定。

# === Edge Function secrets（Supabase Dashboard → Edge Functions → Secrets）===
# 這些「不是」.env、也「不是」Cursor Cloud Agent secrets，是第三個獨立的儲存位置。
# SUPABASE_URL / SUPABASE_ANON_KEY / SUPABASE_SERVICE_ROLE_KEY — Supabase 自動注入
# FACTORY_SERVICE_ROLE_KEY — PMS v3 的 service_role key，大部分 fetch-* function 讀這個（必需）
# MASTER_SERVICE_ROLE_KEY — 同一把 key 的另一個名字（upload-to-master-db / manage-master-media 讀）
# PMS_SSO_SHARED_SECRET — 與 PMS server 共用；mint action 必需
# FURNITURE_APP_URL — 例 https://www.bwteam-furniture.com（mint exchange_url 用）
# RESEND_API_KEY — send-upload-log-report-email（每日 18:00 HKT digest）首選
# UPLOAD_LOG_REPORT_FROM_EMAIL / UPLOAD_LOG_REPORT_TO_EMAIL — 選用寄件者／收件者
# GMAIL_SMTP_USER + GMAIL_SMTP_APP_PASSWORD — RESEND_API_KEY 未設時的 fallback
# SHOPIFY_* / GEMINI_API_KEY — 對應功能用
# 設定腳本：node scripts/setup-upload-log-report-email.mjs（需 SUPABASE_ACCESS_TOKEN）
