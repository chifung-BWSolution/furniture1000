{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "MSG=$(node -e \"let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{try{const o=JSON.parse(d);const f=(o.tool_input||{}).file_path||(o.tool_input||{}).path||'';const b=f.split('/').pop()||'';console.log(b||'update files')}catch{console.log('update files')}})\"); cd \"/c/Users/Administrator/tempo/projects/furniture1000\" && git add -A && git diff --cached --quiet || git commit -m \"auto: $MSG\" && git push origin main 2>/dev/null || true",
            "statusMessage": "Pushing to GitHub..."
          }
        ]
      }
    ]
  }
}
